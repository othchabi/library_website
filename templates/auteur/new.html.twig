{% extends 'layoutgeneral.html.twig' %}

{% block title %}Ajouter auteur{% endblock %}

{% block body %}
    <section class="portfolio-block block-intro">
        <div class="container">
            <div class="card">
                <div class="card-header py-3">
                    <h1 style="text-align: left;">Ajouter un auteur :</h1>
                </div>
                <div class="card-body">
                    {{ include('auteur/_form.html.twig') }}
                    <a href="{{ path('auteur_index') }}">
                        <div class="btn-group" role="group" style="
    margin-top: 10px;
">
                            <button class="btn btn-primary" type="button">Retour à la liste</button>
                            <button class="btn btn-primary" type="button"><i class="fas fa-step-backward"></i></button>
                        </div>
                    </a>
                </div>
            </div>
        </div>
    </section>

{% endblock %}
